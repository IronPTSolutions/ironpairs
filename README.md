# Ironpairs

## Install

Run in your terminal:
```
npm i -g ironpairs
```

## Configuration

Create `.ironhackers.json` in the root of the home directory (`~/`).

```json
[
  "Pablo",
  "Fran",
  "Julio"
]
```